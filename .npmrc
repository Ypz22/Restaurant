# Legacy peer deps required due to @types/node version mismatch between vitest@5.0.1
# (which requires @types/node^22 || >=24) and Next.js TypeScript setup (@types/node^20).
# This allows npm to proceed without manual --legacy-peer-deps flag.
legacy-peer-deps=true
